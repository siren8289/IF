spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/if_spring
    username: if_user
    password: change-me
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    open-in-view: false

logging:
  level:
    org.springframework.sql: debug

