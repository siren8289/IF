
# IF_app-3 (Expo)

  This is a code bundle for IF_app. The original project is available at https://www.figma.com/design/OgjiySW56VoSZ8encUVjl5/IF_app.

## Running the code (Expo)

  Run `npm i` to install the dependencies.

Run `npm run start` to start the Expo dev server.

### Common commands

- `npm run ios`
- `npm run android`
- `npm run web`
  